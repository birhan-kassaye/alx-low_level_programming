#include "main.h"

/**
 * _abs - Write a function that computes the absolute value of an integer
 * @i: an integer
 * Return: returns i if i >= 0, -i if i < 0
 */
int _abs(int i)
{
	if (i >= o)
		return (i);
	else
		return (-i);
}


